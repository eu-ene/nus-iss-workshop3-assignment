# note: if running into issues, can consider to remove the version locking

# Core
requests>=2.28
beautifulsoup4>=4.12
python-dotenv>=1.0
pydantic>=1.10
rich>=13.3.3

# Amadeus SDK for flights
amadeus>=5.3.0

# OpenAI SDK for LLM
openai>=0.27.0

# Optional: LangGraph (adapter included)
langgraph>=0.0.1

# Optional: For Zscaler SSL certification issue
truststore